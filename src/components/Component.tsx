//This is just a temporary component - delete later

interface Props {
  name: string;
  age: number;
  isMarried: boolean;
}

export const Component = (props: Props) => {
  return <div>Component</div>;
};
